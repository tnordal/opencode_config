---
description: Create a new component skeleton
---
Create a new component named $ARGUMENTS.

Requirements:
- TypeScript types if the repo uses TS
- Basic props interface
- Export the component
- Add a minimal test if tests exist
